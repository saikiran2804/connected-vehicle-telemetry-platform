global:
  scrape_interval: 10s

scrape_configs:
  - job_name: telemetry-ingest
    metrics_path: /metrics
    static_configs:
      - targets: ["telemetry-ingest:8000"]

  - job_name: alerts-processor
    metrics_path: /metrics
    static_configs:
      - targets: ["alerts-processor:8000"]
