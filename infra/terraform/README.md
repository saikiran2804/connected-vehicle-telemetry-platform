# Phase 2 — Infrastructure as Code (Terraform)

Placeholder for AWS infrastructure provisioning. Planned scope:

- Modular Terraform: `network` (VPC), `eks`, `ecr`, `iam` (IRSA)
- Remote state: S3 backend + DynamoDB state locking
- Per-environment isolation via workspaces (`dev`, `staging`, `prod`)
- Variables, outputs, and reusable modules

To be built in Phase 2.
